<view class="dx-checkbox {{ size }} {{ color }}">
	<text
		data-value="{{ item.value }}"
		class="item {{ value_[item.value] ? 'odd' : '' }}"
		bindtap="onChecked"
		wx:key="value"
		wx:for="{{ option }}"
	>{{ item.label }}</text>
</view>
